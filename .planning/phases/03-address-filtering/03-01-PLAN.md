---
phase: 03-address-filtering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/popup/App.tsx
autonomous: true
requirements:
  - FILTER-01
must_haves:
  truths:
    - "Popup vault view renders a second freeform search input directly below the existing card-search input"
    - "Typing into the new input updates a dedicated React state variable on every keystroke"
    - "The new input mentions city or state in its placeholder so users understand the match scope"
    - "The new input visually mirrors the existing card-search input (Search icon prefix, same Tailwind classes)"
  artifacts:
    - path: "src/popup/App.tsx"
      provides: "addressSearch useState + second search input rendered below the card-search input in the header strip"
      contains: "addressSearch"
  key_links:
    - from: "src/popup/App.tsx (new input)"
      to: "src/popup/App.tsx (addressSearch state)"
      via: "controlled input value/onChange"
      pattern: "value=\\{addressSearch\\}"
---

<objective>
Add a second freeform search input to the popup vault header — visually identical to the existing card-search input, stacked directly below it — and wire it to a new `addressSearch` React state variable. This plan lands the UI surface only; it does NOT yet change pairing behavior. (Behavior wiring is Plan 03-02.)

Purpose: Address filter UI shipped per D-01/D-02, mirroring the canonical card-search shape (D-13 — same `useState<string>`, no persistence). Closes FILTER-01 (single freeform input, no dropdowns).

Output: `src/popup/App.tsx` modified with one new useState declaration and one new input element rendered in the header strip.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-address-filtering/03-CONTEXT.md
@src/popup/App.tsx
@src/lib/types.ts

<interfaces>
<!-- Existing canonical patterns to mirror verbatim. Do NOT modify these lines — copy their shape. -->

From src/popup/App.tsx:22 (sibling state declaration to add next to):
```typescript
const [searchQuery, setSearchQuery] = useState("");
```

From src/popup/App.tsx:373-382 (canonical input render — mirror this shape exactly for the new input):
```tsx
<div className="relative">
  <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400" />
  <input
    type="text"
    placeholder="Search cards..."
    value={searchQuery}
    onChange={(e) => setSearchQuery(e.target.value)}
    className="w-full pl-9 pr-4 py-2 border rounded-lg text-sm focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none"
  />
</div>
```

From src/lib/types.ts:70-75 (Address shape — confirms `city: string` and `state: string` exist; relevant to Plan 03-02, included here for forward compatibility):
```typescript
export interface Address {
  // ...
  city: string;
  state: string;
  // ...
}
```

`Search` icon is already imported at `src/popup/App.tsx:2` — reuse it. Do NOT add a new lucide-react import.

The header strip container is `src/popup/App.tsx:372` (`<div className="p-4 space-y-3 bg-white border-b">`). `space-y-3` already provides the vertical gap between the two inputs — no extra wrapper needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add addressSearch state declaration</name>
  <files>src/popup/App.tsx</files>
  <read_first>
    - src/popup/App.tsx (lines 14-30: confirm useState pattern and current ordering of state declarations)
    - .planning/phases/03-address-filtering/03-CONTEXT.md §"decisions" (D-13: state lives in a single useState&lt;string&gt; in App.tsx, no persistence)
  </read_first>
  <action>
    Add a new state declaration `const [addressSearch, setAddressSearch] = useState("");` immediately after the existing `searchQuery` declaration at `src/popup/App.tsx:22`. Use the exact name `addressSearch` / `setAddressSearch` (per D-13 — mirror `searchQuery`/`setSearchQuery` shape). No `useMemo`, no default placeholder, no chrome.storage hydration — empty string default per D-13.
  </action>
  <verify>
    <automated>grep -nE "const \[addressSearch, setAddressSearch\] = useState\\(\"\"\\)" src/popup/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "addressSearch" src/popup/App.tsx` returns at least 1
    - `grep -c "setAddressSearch" src/popup/App.tsx` returns at least 1
    - The declaration sits between line 18 (`authView` useState) and line 30 (start of `fetchCards`) — i.e. inside the existing useState cluster
    - No new imports added to the file (App.tsx top-of-file imports unchanged from line 1-10)
  </acceptance_criteria>
  <done>
    `addressSearch` and `setAddressSearch` are declared as `useState&lt;string&gt;` initialized to `""` in `App.tsx`, in the same useState cluster as `searchQuery`. TypeScript build still passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render the second search input below the card-search input</name>
  <files>src/popup/App.tsx</files>
  <read_first>
    - src/popup/App.tsx (lines 367-405: the existing header strip, including `space-y-3` container and the canonical input at 373-382)
    - .planning/phases/03-address-filtering/03-CONTEXT.md §"decisions" (D-01, D-02, D-12)
    - CLAUDE.md §"Tailwind palette" (confirm `focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500` is the canonical input focus ring)
  </read_first>
  <action>
    Insert a new `<div className="relative">…</div>` block IMMEDIATELY AFTER the existing card-search input block (closes at `src/popup/App.tsx:382`) and BEFORE the `Generate New Card` button block that starts at `src/popup/App.tsx:383`. The new block MUST mirror the card-search block (`src/popup/App.tsx:373-382`) verbatim except:
    - `placeholder="Filter addresses by city or state"` (per D-02 — must literally contain the substring "city or state")
    - `value={addressSearch}`
    - `onChange={(e) => setAddressSearch(e.target.value)}`
    Reuse the same `Search` icon import already present at `src/popup/App.tsx:2`. Reuse the same className string verbatim from line 380. Do NOT add an X-of-Y counter (rejected per D-03), do NOT add a clear button, do NOT add a warning banner (D-12).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Filter addresses by city or state" src/popup/App.tsx` returns 1 (placeholder present, exact wording)
    - `grep -c "value={addressSearch}" src/popup/App.tsx` returns 1
    - `grep -c "setAddressSearch(e.target.value)" src/popup/App.tsx` returns 1
    - `grep -c "focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500" src/popup/App.tsx` returns at least 2 (existing card-search input + new address-search input share the same focus ring per CLAUDE.md Tailwind palette)
    - `grep -c "import.*Search.*lucide-react" src/popup/App.tsx` is unchanged (no new lucide-react imports)
    - The order in the rendered JSX is: card-search input (line ~373) → address-search input (new) → `Generate New Card` button → `Autofill Next` button. Verifiable with `grep -n "Search cards\\.\\.\\.\\|Filter addresses by city or state\\|Generate New Card\\|Autofill Next" src/popup/App.tsx` showing line numbers in that order.
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    Loading the popup shows two stacked search inputs in the header strip with identical styling. Typing into the second input updates `addressSearch` state (verifiable via React DevTools or by adding a temporary `console.log(addressSearch)` and removing it). No pairing behavior has changed yet — that ships in Plan 03-02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard → React state | User keystrokes feed `setAddressSearch`; values stay in memory and are never sent to background SW, content script, Express, or Supabase in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `addressSearch` input value | accept | Local-only React state; no persistence (D-13), no IPC, no raw-HTML injection — value is rendered only as a controlled input attribute. |
| T-03-02 | Information Disclosure | `addressSearch` value at rest | accept | No persistence to `chrome.storage`, Supabase, or background SW per D-13; value is discarded on popup close. |
| T-03-03 | Injection | placeholder / value rendering | mitigate | Render `addressSearch` only via a React-controlled input (`value={addressSearch}`), never via raw-HTML escape hatches or DOM string concatenation. React auto-escapes the value when echoed in JSX. |
</threat_model>

<verification>
- `npm run build` exits 0 (no TS or Vite errors)
- `grep -c "addressSearch" src/popup/App.tsx` ≥ 3 (state declaration, input value, input onChange)
- Manual smoke (optional, executor's discretion): `npm run dev`, load unpacked, open popup → confirm two stacked inputs render and the second accepts keystrokes.
</verification>

<success_criteria>
- A second freeform search input renders directly below the existing card-search input in the popup vault view, mirroring its styling.
- The new input is a controlled component bound to a new `addressSearch` `useState<string>` with empty-string default.
- No regressions to the existing card search, Generate New Card button, or Autofill Next button.
- No backend, message-type, or schema changes (FILTER-01, D-14).
</success_criteria>

<output>
After completion, create `.planning/phases/03-address-filtering/03-01-SUMMARY.md` documenting: which lines were inserted in `src/popup/App.tsx`, the final variable name chosen (`addressSearch`), and confirmation that the input is currently inert (no pairing behavior change yet — handed off to Plan 03-02).
</output>
